{
  "name": "batch-ascent-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel deployment wrapper — the real apps live in client/ and server/. This root package.json only exists so Vercel can resolve dependencies for the /api serverless function (api/index.ts), which imports server/src/app directly.",
  "dependencies": {
    "@supabase/supabase-js": "^2.87.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^7.5.0",
    "multer": "^2.0.1",
    "resend": "^6.6.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12",
    "@types/node": "^20",
    "@vercel/node": "^5",
    "typescript": "^5"
  }
}
