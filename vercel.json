{
  "installCommand": "true",
  "buildCommand": "npm --prefix client install --include=dev && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
